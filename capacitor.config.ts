import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tips.betting',
  appName: 'Betting Tips',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
